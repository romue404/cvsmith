<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ name }} - Curriculum Vitae</title>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Open+Sans:ital,wght@0,300..800;1,300..800&family=Cantarell:ital,wght@0,400;0,500;0,600;0,700;1,400&display=swap');

        /* ========== CSS VARIABLES - CUSTOMIZE HERE ========== */
        :root {
            /* ========== COLORS ========== */
            --color-primary: #2a2a2d;
            --color-secondary: #6a6a70;
            --color-accent: #d7d7d9;

            /* ========== TYPOGRAPHY ========== */

            --font-family-primary: 'Open Sans', Arial, sans-serif;
            --font-family-heading: 'Cantarell', 'Open Sans', Arial, sans-serif;
            --font-family-monospace: 'Courier New', monospace;
            /* Root font size - adjust this one value to scale all text */
            --font-size-base: 10px;
            /* Computed sizes from base */
            --font-size-small: calc(var(--font-size-base) * 0.8);
            --font-size-header: 11px;
            --font-size-section-title: calc(var(--font-size-base) * 1.2);
            --font-size-name: 28px;

            /* ========== SPACING ========== */
            --spacing-base: 4px;
            --spacing-xs: var(--spacing-base);
            --spacing-sm: calc(var(--spacing-base) * 1.5);
            --spacing-md: calc(var(--spacing-base) * 2);
            --spacing-lg: calc(var(--spacing-base) * 3);
            --spacing-xl: calc(var(--spacing-base) * 6.25);

            /* ========== TIMELINE ========== */
            --timeline-dot-size: 8px;
            --timeline-dot-size-nested: 4px;
            --timeline-line-width: 1px;
            --timeline-start-offset: 53px;

            /* ========== LAYOUT ========== */
            --timeline-position: 55px;
            --timeline-dot-position: calc(var(--timeline-position) + var(--timeline-line-width) / 2 - var(--timeline-dot-size) / 2);
            --profile-photo-size: 100px;
            --section-grey-block-width: 31mm;
            --page-padding-horizontal: 20mm;
            --page-padding-vertical: 12mm;
            --entry-date-width: 52px;
            --entry-gap: 25px;
            --header-gap: 25px;
            --nested-dot-offset: calc(var(--timeline-position) - var(--timeline-dot-size-nested) / 2 - var(--entry-date-width) - var(--entry-gap) - var(--spacing-lg));



            /* ========== SIZING ========== */
            --profile-photo-border: 2px;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        @page {
            size: {{ page_css_size }};
            margin: 0;
        }

        html {
            width: 100%;
            margin: 0;
            padding: 0;
        }

        body {
            font-family: var(--font-family-primary);
            color: var(--color-primary);
            background: #fafafa;
            line-height: 1.5;
            -webkit-font-smoothing: antialiased;
            -moz-osx-font-smoothing: grayscale;
            margin: 0;
            padding: 0;
            width: 100%;
            max-width: 230mm;
            margin: 0 auto;
        }

        .page {
            width: {{ page_width }};
            height: auto;
            background: white;
            padding: var(--page-padding-vertical) var(--page-padding-horizontal);
            overflow: visible;
            position: relative;
            margin: 12px auto;
            box-shadow: 0 0 12px rgba(0, 0, 0, 0.08);
        }

        /* ============ HEADER SECTION ============ */
        .header-section {
            display: grid;
            grid-template-columns: var(--profile-photo-size) 1fr;
            gap: var(--header-gap);
            align-items: flex-start;
            margin-bottom: calc(var(--spacing-lg));
            padding-bottom: var(--spacing-xl);
        }

        .header-section.no-photo {
            grid-template-columns: 1fr;
            gap: 0;
        }

        .profile-photo {
            width: var(--profile-photo-size);
            height: var(--profile-photo-size);
            border-radius: 50%;
            overflow: hidden;
            /* ← this is what makes it a circle */
            border: var(--profile-photo-border) solid var(--color-accent);
            display: block;
            /* avoid flex weirdness */
        }

        .profile-photo img {
            width: 100%;
            height: 100%;
            object-fit: cover;
            /* fills the circle */
            display: block;
            /* removes inline gap */
        }

        .header-center {
            text-align: center;
            display: flex;
            flex-direction: column;
            align-items: center;
        }

        .header-subtitle {
            font-size: var(--font-size-header);
            letter-spacing: 4px;
            color: var(--color-secondary);
            text-transform: uppercase;
            margin-bottom: var(--spacing-md);
            font-weight: 500;
            font-family: var(--font-family-primary);
        }

        .header-name {
            font-size: var(--font-size-name);
            font-weight: 700;
            color: var(--color-primary);
            letter-spacing: -0.5px;
            font-family: var(--font-family-primary);
        }

        .personal-info {
            font-size: var(--font-size-header);
            color: var(--color-secondary);
            display: flex;
            align-items: center;
            justify-content: center;
            gap: var(--spacing-sm);
            margin-top: var(--spacing-md);
            flex-wrap: wrap;
            line-height: 1.8;
        }

        .info-piece {
            display: inline-block;
            white-space: nowrap;
            margin: 0;
            padding: 0;
            padding-right: var(--spacing-md);
        }

        .info-piece a.cv-link {
            color: inherit;
            text-decoration: none;
        }


        /* ============ ABOUT ME SECTION ============ */
        .about-me-section {
            margin-bottom: var(--spacing-xl);
            position: relative;
        }

        .about-me-content {
            font-family: var(--font-family-primary);
            font-size: var(--font-size-base);
            color: #555;
            line-height: 1.6;
            text-align: justify;
        }

        /* ============ TIMELINE LAYOUT ============ */
        .timeline-container {
            position: relative;
            margin-bottom: var(--spacing-xl);
        }

        /* ============ SECTIONS ============ */
        .section {
            margin-bottom: var(--spacing-xl);
            position: relative;
            z-index: 1;
        }

        /* Vertical timeline line: only for sections inside the timeline container */
        .timeline-container .section::before {
            content: '';
            position: absolute;
            left: var(--timeline-position);
            top: var(--timeline-start-offset);
            bottom: 0;
            width: var(--timeline-line-width);
            background: var(--color-accent);
            z-index: 0;
        }

        .section-header {
            display: flex;
            align-items: center;
            gap: 0;
            margin-bottom: calc(var(--spacing-xl) * 0.5);
            margin-left: calc(-1 * var(--page-padding-horizontal));
            margin-right: calc(-1 * var(--page-padding-horizontal));
            padding-left: 0;
            padding-right: var(--page-padding-horizontal);
        }

        /* Grey block reaches exactly to the timeline at timeline position */
        .section-grey-block {
            width: var(--section-grey-block-width);
            height: 20px;
            background: var(--color-accent);
            flex-shrink: 0;
        }

        .section-title {
            font-family: var(--font-family-heading);
            font-size: var(--font-size-section-title);
            font-weight: 700;
            text-transform: uppercase;
            letter-spacing: 1.2px;
            color: var(--color-primary);
            position: relative;
            z-index: 2;
            margin-left: var(--spacing-lg);
        }

        /* ============ ENTRIES ============ */
        .entry {
            display: grid;
            grid-template-columns: var(--entry-date-width) 1fr;
            gap: var(--entry-gap);
            position: relative;
            margin-bottom: var(--spacing-lg);
        }

        /* Small black dot on timeline */
        .entry::before {
            content: '';
            position: absolute;
            left: var(--timeline-dot-position);
            top: 5px;
            width: var(--timeline-dot-size);
            height: var(--timeline-dot-size);
            border-radius: 50%;
            background: var(--color-primary);
            z-index: 3;
        }

        .entry-date {
            font-size: var(--font-size-base);
            color: var(--color-secondary);
            text-align: left;
            padding-top: var(--spacing-xs);
            line-height: 1.5;
            font-weight: 500;
            margin-right: var(--spacing-md);
        }

        .entry-content {
            padding-left: 0;
            padding-top: 0;
        }

        .entry-header {
            display: flex;
            align-items: baseline;
            gap: var(--spacing-sm);
            margin-bottom: var(--spacing-sm);
            flex-wrap: wrap;
        }

        .entry-title {
            font-size: var(--font-size-header);
            font-weight: 700;
            color: var(--color-primary);
            font-family: var(--font-family-primary);
        }

        .entry-separator {
            color: var(--color-secondary);
            font-size: var(--font-size-header);
        }

        .entry-subtitle {
            font-size: var(--font-size-base);
            color: var(--color-secondary);
        }

        .entry-items {
            font-size: var(--font-size-base);
            color: #555;
            line-height: 1.5;
        }

        .entry-item {
            display: flex;
            gap: var(--spacing-sm);
            margin-bottom: var(--spacing-xs);
        }

        .entry-item::before {
            content: '›';
            color: var(--color-primary);
            font-weight: bold;
            flex-shrink: 0;
            font-size: var(--font-size-small);
            margin-top: 1px;
        }

        .technologies {
            font-size: var(--font-size-base);
            color: var(--color-secondary);
            margin-top: var(--spacing-sm);
            font-family: var(--font-family-monospace);
        }

        /* ============ NESTED POSITIONS ============ */

        .nested-positions {
            margin-top: var(--spacing-lg);
            padding-left: var(--spacing-lg);
        }

        .nested-position {
            margin-bottom: var(--spacing-md);
            position: relative;
        }

        .nested-position:last-child {
            margin-bottom: 0;
        }

        /* Small dot on timeline for nested positions */
        .nested-position::before {
            content: '';
            position: absolute;
            left: var(--nested-dot-offset);
            top: 3px;
            width: var(--timeline-dot-size-nested);
            height: var(--timeline-dot-size-nested);
            border-radius: 50%;
            background: var(--color-primary);
            z-index: 3;
        }

        .nested-position-header {
            display: flex;
            align-items: baseline;
            gap: var(--spacing-sm);
            margin-bottom: var(--spacing-sm);
            flex-wrap: wrap;
        }

        .nested-position-period {
            font-size: var(--font-size-small);
            color: var(--color-secondary);
            font-weight: 600;
            background: var(--color-accent);
            padding: 2px 6px;
            border-radius: 2px;
            flex-shrink: 0;
            line-height: 1.4;
            display: inline-block;
        }

        .nested-position-title {
            font-size: var(--font-size-base);
            font-weight: 400;
            color: var(--color-primary);
            font-family: var(--font-family-primary);
        }

        .nested-position .entry-items {
            padding-left: var(--spacing-lg);
        }

        /* ============ SKILLS SECTION ============ */
        .skills-container {
            display: flex;
            flex-wrap: wrap;
            gap: var(--spacing-xl);
            justify-content: space-between;
        }

        .skill-category:nth-child(n+4) {
            display: none;
        }

        .skill-category {
            margin-bottom: var(--spacing-xl);
            padding: var(--spacing-lg);
            border-radius: 4px;
        }

        .skill-category-title {
            font-family: var(--font-family-heading);
            font-weight: 700;
            font-size: var(--font-size-base);
            color: var(--color-primary);
            margin-bottom: var(--spacing-sm);
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }

        .skill-item {
            display: flex;
            align-items: center;
            font-size: var(--font-size-base);
            margin-bottom: var(--spacing-xs);
            color: #555;
            gap: var(--spacing-xs);
        }

        .skill-item span:first-child {
            margin-right: var(--spacing-md);
        }

        .skill-level {
            font-size: var(--font-size-small);
            letter-spacing: 2.5px;
            color: var(--color-primary);
            margin-left: auto;
        }

        /* ============ FOOTER ============ */
        .footer {
            position: absolute;
            bottom: var(--page-padding-vertical);
            right: var(--page-padding-horizontal);
            font-size: var(--font-size-small);
            color: var(--color-secondary);
        }

        /* ============================================================================
           PRINT OVERRIDES — keeps screen look, fixes PDF/print
           ========================================================================== */

        @media print {
            @page {
                size: {{ page_css_size }};
                margin: 12mm 0;
            }

            html,
            body {
                background: #fff !important;
                margin: 0 !important;
                padding: 0 !important;
                width: 100% !important;
            }

            * {
                -webkit-print-color-adjust: exact !important;
                print-color-adjust: exact !important;
            }

            .page {
                width: {{ page_width }} !important;
                margin: 0 !important;
                box-shadow: none !important;
                background: #fff !important;
                padding: 0 var(--page-padding-horizontal) !important;
                overflow: visible !important;
                break-inside: auto !important;
                page-break-inside: auto !important;
                page-break-after: auto !important;
            }

            h1,
            h2,
            h3,
            h4,
            h5,
            h6,
            .section-title,
            .header-name {
                break-after: avoid-page;
                page-break-after: avoid;
                orphans: 3;
                widows: 3;
            }

            h1+*,
            h2+*,
            h3+*,
            .section-header+* {
                break-before: avoid-page;
                page-break-before: avoid;
            }

            .section-header,
            .entry-header,
            .nested-position-header {
                break-after: avoid-page;
                page-break-after: avoid;
            }

            .header-section,
            .about-me-section,
            .award-item,
            .skill-category,
            figure {
                break-inside: avoid;
                page-break-inside: avoid;
            }

            .entry,
            .entry-nested,
            .nested-position,
            .entry-item,
            .technologies {
                break-inside: avoid;
                page-break-inside: avoid;
            }

            img,
            svg,
            canvas {
                max-width: 100% !important;
                height: auto !important;
                break-inside: avoid;
                page-break-inside: avoid;
            }

            .page-break {
                break-before: page;
                page-break-before: always;
            }

            .footer {
                position: static !important;
                margin-top: 6mm !important;
                text-align: right !important;
            }

            thead {
                display: table-header-group;
            }

            tr,
            td,
            th {
                break-inside: avoid;
                page-break-inside: avoid;
                overflow-wrap: break-word;
            }

            a[href]::after {
                content: "" !important;
            }
        }
    </style>
</head>

<body>
    <div class="page">
        <!-- HEADER -->
        <header class="header-section{% if not photo %} no-photo{% endif %}">
            {% if photo %}
            <div class="profile-photo">
                <img src="{{ photo }}" alt="Profile photo" />
            </div>
            {% endif %}

            <div class="header-center">
                <div class="header-subtitle">Curriculum Vitae</div>
                <div class="header-name">{{ name }}</div>

                <div class="personal-info">
                    {% if personal_info.info %}
                    {% for item in personal_info.info %}
                    <span class="info-piece">{{ item|linkify|safe }}</span>
                    {% endfor %}
                    {% else %}
                    {# Fallback for legacy structure #}
                    {% if personal_info.birth %}
                    <span class="info-piece">
                        <span class="info-icon">◆</span>
                        <span class="info-text">{{ personal_info.birth }}</span>
                    </span>
                    {% endif %}

                    {% if personal_info.address %}
                    <span class="info-piece">
                        <span class="info-icon">⌂</span>
                        <span class="info-text">{{ personal_info.address }}</span>
                    </span>
                    {% endif %}

                    {% if personal_info.phone %}
                    <span class="info-piece">
                        <span class="info-icon">☎</span>
                        <span class="info-text">{{ personal_info.phone }}</span>
                    </span>
                    {% endif %}

                    {% if personal_info.email %}
                    <span class="info-piece">
                        <span class="info-icon">✉</span>
                        <span class="info-text">{{ personal_info.email }}</span>
                    </span>
                    {% endif %}

                    {% if personal_info.website %}
                    <span class="info-piece">
                        <span class="info-icon">◉</span>
                        <span class="info-text">{{ personal_info.website }}</span>
                    </span>
                    {% endif %}
                    {% endif %}
                </div>
            </div>
        </header>

        <!-- ABOUT ME SECTION (if present) -->
        {% if about_me %}
        <section class="about-me-section">
            <header class="section-header">
                <div class="section-grey-block"></div>
                <div class="section-title">ABOUT ME</div>
            </header>
            <div class="about-me-content">
                {{ about_me }}
            </div>
        </section>
        {% endif %}

        <!-- MAIN CONTENT WITH TIMELINE -->
        <div class="timeline-container">
            <!-- DYNAMIC SECTIONS -->
            {% for section in sections %}
            <section class="section">
                <header class="section-header">
                    <div class="section-grey-block"></div>
                    <div class="section-title">{{ section.name }}</div>
                </header>

                {% for entry in section.entries %}
                <div class="entry">
                    <div class="entry-date">{{ entry.period|replace(' – ', ' –<br>')|safe }}</div>
                    <div class="entry-content">
                        <div class="entry-header">
                            <span class="entry-title">{{ entry.title }}</span>
                            <span class="entry-separator"> | </span>
                            <span class="entry-subtitle">{{ entry.subtitle }}</span>
                        </div>
                        <div class="entry-items">
                            {% for detail in entry.details %}
                            <div class="entry-item">{{ detail|safe }}</div>
                            {% endfor %}
                        </div>



                        {% if entry.positions %}
                        <div class="nested-positions">
                            {% for position in entry.positions %}
                            <div class="nested-position">
                                <div class="nested-position-header">
                                    <span class="nested-position-period">{{ position.period }}</span>
                                    <span class="nested-position-title">{{ position.title }}</span>
                                </div>
                                <div class="entry-items">
                                    {% for detail in position.details %}
                                    <div class="entry-item">{{ detail|safe }}</div>
                                    {% endfor %}
                                </div>
                            </div>
                            {% endfor %}
                        </div>
                        {% endif %}
                        {% if entry.technologies %}
                        <div class="technologies">
                            ⚙ {{ entry.technologies|join(', ') }}
                        </div>
                        {% endif %}

                    </div>
                </div>
                {% endfor %}
            </section>
            {% endfor %}
        </div>

        <!-- SKILLS SECTION -->
        {% if skills %}
        <section class="section">
            <header class="section-header">
                <div class="section-grey-block"></div>
                <div class="section-title">TECHNICAL SKILLS</div>
            </header>
            <div class="skills-container">
                {% for category in skills %}
                <div class="skill-category">
                    <div class="skill-category-title">{{ category.category }}</div>
                    {% for item in category.skills %}
                    <div class="skill-item">
                        <span>{{ item.name }}</span>
                        <span class="skill-level">{{ '●' * item.level }}{{ '○' * (5 - item.level) }}</span>
                    </div>
                    {% endfor %}
                </div>
                {% endfor %}
            </div>
        </section>
        {% endif %}

        <footer class="footer">
            {{ location }}, {{ today_date }}
        </footer>
    </div>
</body>

</html>